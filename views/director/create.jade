//
   Created by fabiancote on 7/22/14.

h1 Asignar director
h2 programa: #{programa.nombre}
-if (bite==true){
    p Este programa tiene un director asignado, si crea uno nuevo el anterior sera eliminado
-}

form(method="POST", action="/director/create")
    input(type="hidden", name="programa", value="#{programa.id}")

    legend
        | nombres del director
        input(type="text", name="nombres", autocomplete="off", required)
    legend
        | apellidos del director
        input(type="text", name="apellidos", autocomplete="off", required)
    legend
        | tipo de documento
        select(name="tipodoc")
            option(value="Cedula de ciudadania") Cedula de ciudadania
            option(value="Pasaporte") Pasaporte
            option(value="Cedula de Extranjeria") Cedula de Extranjeria
    legend
        | numero de documento
        input(type="text", name="numdoc", autocomplete="off", required)
    legend
        | Email
        input(type="email", name="email", autocomplete="off", required)
    legend
        | telefono
        input(type="number", name="telefono", autocomplete="off", required)
    input(type="submit", value="crear")

a(href="/programa/#{programa.id}/view") Cancelar

br